import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  home: {
  	flex:8,
  	justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#fff',
  },
  page:{
  	color:"#000"
  }
});
