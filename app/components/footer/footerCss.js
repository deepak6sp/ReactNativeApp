import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  footer: {
  	flex:1,
    flexDirection:'row',
    maxHeight:60
  },
  footerText:{
    flex:1,
  	color:"#fff",
    backgroundColor:'#333',
    textAlignVertical:'center',
    textAlign:'center'
  }
});
